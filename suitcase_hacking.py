def guess_code(password: str):
    pasword_list = list(password)
    for n in range 5:
        code = input("")
        code_list = list[code]
        number = 0
        if pasword_list[n] == code_list[n]
            



        if
        for i in range code:


        if number < random_number:
            print(f"Неверно! Число больше {number}")
        elif number > random_number:
            print(f"Неверно! Число меньше {number}")
        else:
            print("Верно!")
            break

